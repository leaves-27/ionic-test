var app = angular.module('courier',[]);
  // app.controller('LoginController', function ($scope, $rootScope, AUTH_EVENTS, AuthService) {
app.controller('CourierController', function ($scope, $rootScope) {  
  
});